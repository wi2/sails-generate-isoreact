var React = require('react')
  , Layout = require('../layout.js').Layout;

module.exports = React.createClass({
  render: function() {
    return (
      <Layout>
        <h1>HomePage</h1>
      </Layout>
    );
  }
});
